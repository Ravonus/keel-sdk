Create a production-ready pixel-art macro floor atlas for a top-down twin-stick roguelike dungeon, strict square 1024x1024 image. Divide the canvas into exactly four equal 512x512 quadrants (2 columns x 2 rows), with boundaries exactly at x=512 and y=512. Each quadrant is a COMPLETE CONTINUOUS TOP-DOWN ORTHOGRAPHIC FLOOR SLAB, not a collection of separate tiles. Style: grayscale occult science-fiction machine crypt, high-quality readable pixel art, hard pixel edges, no perspective, no characters, no props, no walls, no doors, no text, no labels, no gutters, no outer frame. All four slabs must share the same neutral charcoal/silver material and lighting so runtime tinting works. Quadrant A: a calm industrial panel field with a large centered eight-point circuit seal spanning most of the slab, connected conduits reaching each edge. Quadrant B: a cathedral-machine floor with a large concentric reactor ring and four radial paths reaching each edge. Quadrant C: a branching circuit labyrinth with broad calm walking lanes and a strong asymmetrical authored composition. Quadrant D: a ritual warning grid with one large offset diamond sigil, perimeter cable channels, and deliberate negative space. Important: each 512x512 quadrant must read as one hand-authored room-scale composition when cut into an 8x8 grid of 64x64 cells and reconstructed. Lines crossing 64-pixel cell boundaries must remain perfectly continuous. Avoid one-symbol-per-cell repetition and avoid checkerboards. The four quadrant boundaries may be clean straight seams but contain no padding. Candidate asset only, for visual review.
