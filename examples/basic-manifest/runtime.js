const canvas=document.querySelector('#canvas');const context=canvas.getContext('2d');let width,height,time=0;function resize(){width=canvas.width=innerWidth*devicePixelRatio;height=canvas.height=innerHeight*devicePixelRatio}addEventListener('resize',resize);resize();function render(){time+=.008;context.fillStyle='rgba(9,9,11,.22)';context.fillRect(0,0,width,height);for(let index=0;index<80;index++){const angle=index*.55+time*(1+index%5)/4;const radius=(40+index*7)*devicePixelRatio;const x=width*.66+Math.cos(angle)*radius;const y=height*.42+Math.sin(angle*.91)*radius*.62;context.beginPath();context.arc(x,y,(1.2+(index%4)) * devicePixelRatio,0,Math.PI*2);context.fillStyle=`rgba(255,182,193,${.15+(index%9)/14})`;context.fill()}requestAnimationFrame(render)}render();
